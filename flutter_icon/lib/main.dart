import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: new Container(
        height: 200.0,
        width: 200.0,
        child: new Icon(Icons.flag),
        foregroundDecoration: new ,
      )
    );

  }
}
